publishing {
    repositories {
        maven {
            url  = 'https://dl.bintray.com/sandyaraperes/maven'
        }
    }

    publications {
        mavenProject(MavenPublication) {
            groupId project.group
            artifactId project.name
            version project.version

/*
            if (project.name.endsWith('android')) {
                artifact("$buildDir/outputs/aar/anccessos-android-debug.aar")
            }
            artifact sourceJar

            artifact emptyJar {
                classifier 'javadoc'
            }
*/

            withPom(pom)
        }
    }
}
